const student = {
    name: 'Jonas',
    age: 99,
    isMaried: true,
};
const noriuSuzinoti = "age";

const name = student[noriuSuzinoti];

console.log(name);

console.clear();

function testEven(n) {
    (n = n * 0.5 % === 0) ? true : false;

}